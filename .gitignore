# virtualenv
.venv/
venv/

# python
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# raw bulk downloads -- never commit these (SEC quarterly zips are ~85 MB each)
data/raw/
data/raw_sec/
*.zip

# large intermediate caches (per-quarter extracts; rebuilt by src/sec_loader.py)
data/cache/

# result tables under data/ ARE committed -- drop_log.csv, main_test_results.csv,
# robustness_results.csv and sec_quarter_log.csv are deliverables, not caches.

# OS
.DS_Store

# generated intermediate for the writeup PDF
writeup/*.print.html

# Personal study guide -- kept local, not published.
# Regenerate any time with: python -m src.export_docs
writeup/project_guide.*
